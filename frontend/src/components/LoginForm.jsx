import styled from "styled-components";
const Wrapper = styled.div``;
const LoginForm = () => {
  return (
    <Wrapper>
      <legend>Login</legend>
      <form action=""></form>
    </Wrapper>
  );
};

export default LoginForm;
